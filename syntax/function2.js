console.log(Math.round(1.6)); //2
console.log(Math.round(1.4)); //1

function sum(first, second){ // parameter
  return first+second;
  //return은 어떤 함수를 출력시킨다는 의미와 그 즉시, 함수를 종료한다는 두가지 의미를 가진 특수한 키워드
}

console.log(sum(2,4)); // argument
